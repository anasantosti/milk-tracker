spring.application.name=milk-tracker
spring.datasource.url=jdbc:mysql://localhost:3306/milk_tracker?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=CR001*lt

server.port=8081

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Core Properties
audit.storage.type=jpa

# Security is part of the reference application, not an optional showcase mode.
core.security.mode=AUTO
#core.security.oauth2.audiences=${CORE_SECURITY_OAUTH2_AUDIENCES:milk-tracker}
#core.security.oauth2.authorities.claim-paths=${CORE_SECURITY_OAUTH2_AUTHORITIES_CLAIM_PATHS:resource_access.milk-tracker.roles,realm_access.roles}
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${CORE_IDP_JWK_SET_URI:http://localhost:8080/realms/core-realm/protocol/openid-connect/certs}